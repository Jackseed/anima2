import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anima2',
  appName: 'anima2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
